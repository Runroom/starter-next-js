'use client';

import { HydrationBoundary, HydrationBoundaryProps } from '@tanstack/react-query';

const RQHydrate = (props: HydrationBoundaryProps) => <HydrationBoundary {...props} />;

export { RQHydrate };
